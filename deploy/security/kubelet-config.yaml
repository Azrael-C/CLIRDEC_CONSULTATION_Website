# This is a node-level KubeletConfiguration fragment, not a workload manifest.
# Apply it through the cluster provider's supported node-pool configuration.
apiVersion: kubelet.config.k8s.io/v1beta1
kind: KubeletConfiguration
podPidsLimit: 128
